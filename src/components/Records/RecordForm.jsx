import React from "react";
import Form from "../Form";

const RecordForm = () => {
  return <form>
    <select>
        <option value=""></option>
    </select>
  </form>;
};

export default RecordForm;
